import ProfileView from '@/views/dashboard/profile/profile-view'

// ----------------------------------------------------------------------

export const metadata = {
  title: 'Profile',
}

export default function ProfilePage() {
  return <ProfileView />
}